---
layout: base.njk
title: In the News
description: News, media coverage, and recognition of the Boston (MA) Alumni Chapter of Kappa Alpha Psi.
permalink: /news/
---

<section class="hero">
  <div class="container">
    <h1>In the News</h1>
    <p class="tagline">Media Coverage & Recognition</p>
  </div>
</section>

{% if news.recognition.length > 0 %}
<section class="page-section">
  <div class="container">
    <div class="section-header">
      <h2>Recognition & Awards</h2>
    </div>

    {% for item in news.recognition %}
    <article style="margin-bottom: var(--spacing-xl);">
      <h3>{{ item.title }}</h3>
      <p><strong>{{ item.date | formatDate }}</strong> — Presented by {{ item.presenter }}</p>

      <div class="card-grid mt-2">
        {% for image in item.images %}
        <div class="text-center">
          {% image image.src, image.alt, "(max-width: 768px) 100vw, 600px", [300, 450, 600], "lazy", null, null %}
          <p style="font-size: 0.875rem; color: var(--gray); margin-top: var(--spacing-xs);">{{ image.caption }}</p>
        </div>
        {% endfor %}
      </div>

      <blockquote style="background: var(--cream); padding: var(--spacing-md); border-left: 4px solid var(--crimson); margin-top: var(--spacing-md); font-style: italic;">
        {{ item.fullText }}
      </blockquote>
    </article>
    {% endfor %}
  </div>
</section>
{% endif %}

<section class="page-section">
  <div class="container">
    <div class="section-header">
      <h2>Media Coverage</h2>
    </div>

    <div class="card-grid">
      {% for article in news.articles %}
      <div class="card">
        <div class="card-content">
          <h3 class="card-title">{{ article.title }}</h3>
          <p style="font-size: 0.875rem; color: var(--gray);">{{ article.source }} — {{ article.date | formatDate }}</p>
          <p class="card-text">{{ article.excerpt }}</p>
          <a href="{{ article.url }}" target="_blank" rel="noopener noreferrer" class="btn btn-secondary mt-1 external-link">Read Full Article</a>
        </div>
      </div>
      {% endfor %}
    </div>
  </div>
</section>

<div class="text-center" style="padding: var(--space-8) 0;">
  <a href="#top" class="back-to-top">↑ Back to Top</a>
</div>
