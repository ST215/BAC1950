---
layout: base.njk
title: Past Polemarchs
description: All {{ site.polemarchsCount }} Polemarchs who have led the Boston (MA) Alumni Chapter of Kappa Alpha Psi since 1950.
permalink: /chapter/past-polemarchs/
---

<section class="hero">
  <div class="container">
    <h1>Past Polemarchs</h1>
    <p class="tagline">{{ site.polemarchsCount }} Brothers Have Led Our Chapter</p>
  </div>
</section>

<section class="page-section">
  <div class="container">
    <div class="section-header">
      <p>{{ polemarchs.note }}</p>
      <p class="legend"><span class="deceased-marker">*</span> indicates deceased</p>
    </div>

    <div class="polemarchs-grid">
      {% for pol in polemarchs.polemarchs %}
      <div class="polemarch-card{% if pol.deceased %} deceased{% endif %}">
        {% if pol.image %}
        <div class="polemarch-photo">
          {% image pol.image, "Brother " + pol.name, "200px", [200, 400], "lazy", null, null %}
        </div>
        {% endif %}
        <div class="polemarch-number">{{ pol.number | ordinal }}</div>
        <h3 class="polemarch-name">{{ pol.name }}{% if pol.deceased %}<span class="deceased-marker">*</span>{% endif %}</h3>
        <p class="polemarch-term">{{ pol.term }}</p>
        <p class="polemarch-initiation">{{ pol.initiatingChapter }}</p>
        <p class="polemarch-date">{{ pol.initiationDate }}</p>
        {% if pol.notable %}<p class="polemarch-notable">{{ pol.notable }}</p>{% endif %}
      </div>
      {% endfor %}
    </div>
  </div>
</section>

<div class="text-center" style="padding: var(--space-8) 0;">
  <a href="#top" class="back-to-top">↑ Back to Top</a>
</div>
