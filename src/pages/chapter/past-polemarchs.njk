---
layout: base.njk
title: Past Polemarchs
description: All 36 Polemarchs who have led the Boston (MA) Alumni Chapter of Kappa Alpha Psi since 1950.
permalink: /chapter/past-polemarchs/
---

<section class="hero">
  <div class="container">
    <h1>Past Polemarchs</h1>
    <p class="tagline">{{ site.polemarchsCount }} Brothers Have Led Our Chapter</p>
  </div>
</section>

<section class="page-section">
  <div class="container">
    <p class="text-center">{{ polemarchs.note }}</p>
    <p class="text-center"><em>{{ polemarchs.deceasedMarker }} indicates deceased</em></p>
    <a href="#top" class="back-to-top">↑ Back to Top</a>
  </div>
</section>

<section class="page-section">
  <div class="container">
    <table class="data-table">
      <thead>
        <tr>
          <th>#</th>
          <th>Name</th>
          <th>Term</th>
          <th>Initiating Chapter</th>
          <th>Initiation Date</th>
        </tr>
      </thead>
      <tbody>
        {% for pol in polemarchs.polemarchs %}
        <tr>
          <td>{{ pol.number }}</td>
          <td>
            {{ pol.name }}{% if pol.deceased %}*{% endif %}
            {% if pol.notable %}<br><small style="color: var(--gray);">{{ pol.notable }}</small>{% endif %}
          </td>
          <td>{{ pol.term }}</td>
          <td>{{ pol.initiatingChapter }}</td>
          <td>{{ pol.initiationDate }}</td>
        </tr>
        {% if loop.index % 12 == 0 %}
        </tbody>
      </table>
      <a href="#top" class="back-to-top">↑ Back to Top</a>
      <table class="data-table" style="margin-top: var(--spacing-lg);">
        <thead>
          <tr>
            <th>#</th>
            <th>Name</th>
            <th>Term</th>
            <th>Initiating Chapter</th>
            <th>Initiation Date</th>
          </tr>
        </thead>
        <tbody>
        {% endif %}
        {% endfor %}
      </tbody>
    </table>
    <a href="#top" class="back-to-top">↑ Back to Top</a>
  </div>
</section>
